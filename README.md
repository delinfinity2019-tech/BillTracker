
# 🚀 BillTracker - Smart Bill Management

A production-ready financial utility built with React, Tailwind CSS, and Lucide.

## 📦 Deployment to Vercel

This project is pre-configured for **Vercel**. 

1. **Push to GitHub**: Push your local changes to a new repository.
2. **Connect Vercel**: Import the repository into your Vercel dashboard.
3. **Environment Variables**: Add `API_KEY` in the Vercel Project Settings if you plan to enable AI-powered insights.
4. **Deploy**: Vercel will automatically detect the `vercel.json` and `package.json` to build the app.

## 🛠️ Tech Stack

- **React 19** (Standard Frontend)
- **Vite** (Build Tool)
- **Tailwind CSS** (Styling)
- **Lucide React** (Iconography)
- **Recharts** (Financial Visualization)

## 🔐 Security

- Role-based access control (Admin vs User)
- Secure session handling via local storage persistence
- Client-side input validation
